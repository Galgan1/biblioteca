# Dependências para rodar o mural de testes (Akita etapa 5 / CI).
# O core do pipeline e os testes usam stdlib; a única dependência externa
# exercida pelos testes é o pydantic (contracts.py).
pydantic>=2,<3

# parallax 2.5D NORMAL — puxa torch, idealmente precisa de GPU
# (sem GPU, o render cai para CPU e é muito lento; o pipeline degrada para Ken Burns)
depthflow>=1.0.0
